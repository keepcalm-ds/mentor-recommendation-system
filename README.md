# Mentor-Mentee Matching System

## 0. ОБЗОР ПРОЕКТА
Mentor-Mentee Matching System - рекомендательная система которая автоматически подбирает топ-5 менторов для каждого менти. Алгоритм оценивает каждую пару по четырём факторам: схожесть навыков, близость IT-областей, стаж и рейтинг ментора. Пользователь настраивает приоритеты через слайдеры (1–5), система рассчитывает индивидуальные веса.

Структура пайплайна:
| Тетрадка | Входные файлы | Выходные файлы |
| :--- | :---: | ---: |
| 1_ontology.ipynb | mentees.csv, mentors.csv | ontology_domains.csv, ontology_role_domain_mapping.csv, ontology_domain_similarity.csv | 
| 2_preprocessing.ipynb | +ontology_*.csv | mentees_processed.csv, mentors_processed.csv, skill_vocabulary.csv |
| 3_scoring.ipynb | + *_processed.csv, mentor_ratings.csv | normalization_bounds.csv, mentee_weights.csv, recommendations.csv |
| 4_evaluation.ipynb | + все предыдущие | evaluation_results.csv |
| 5_explainability.ipynb | + все предыдущие | recommendations_explained.csv |
| 6_boost_analytics.ipynb | + все предыдущие | boost_analytics.csv |

## 1. ДАННЫЕ И ИХ ОСОБЕННОСТИ
### 1.1 Состав датасета
| Файл | Строк | Сожержание | 
| :--- | :---: | ---: |
| mentees.csv | 5000 | профили менти: level, domain, profession, skills, goal, language, format |
| mentors.csv | 2000 | профили менторов: + experience_years, available, boosted, boost_k |
| mentors_ratings.csv | 2000 | рейтинги: N(3.8, 0.7) в диапазоне [1, 5] |

### 1.2 Пропуски в данных
~35% менти и ~32% менторов имеют хотя бы один пустой атрибут. Это реалистичная ситуация для любой платформы - пользователи не заполняют профили полностью.

*Почему не удаляем профили с пропусками? Потому что это исключило бы треть пользователей. Вместо этого применяем честное наказание: система даёт рекомендации, но score ниже — стимул заполнить профиль.*

### 1.3 Структура данных о менторах 

| Поле | Описание | Использование |
| :--- | :---: | ---: |
| level | junior/middle/senior/lead | Только в жесткой фильтрации - не в формуле score |
| skills | Навыки через ';' | Jaccard similarity с навыками менти |
| experience_years | Стаж в годах | Нормализуется: log(years+1)/log(21) |
| domain + profession | IT-область и должность | domain_vector через онтологию |
| available | Доступен ли для менторства | Жесткая фильтрация |
| boosted + boost_k | Платное продвижение | Буст финального score, если organic >= 0.3 |

## 2. Онтология (тетрадка 1_ontology.ipynb)
Онтология строится автоматически из данных — никаких ручных словарей. Это важно: если добавить новые профессии или домены, достаточно перезапустить тетрадку 1.

Три шага построения
•	Skill - Domain индекс: для каждого навыка считаем как часто он встречается у пользователей каждой области. Нормируем — получаем распределение навыка по областям.
•	Role - Domain mapping: для каждой профессии агрегируем навыки всех ее носителей через skill - domain индекс. Результат — вектор из 10 весов, показывающий насколько профессия близка к каждой области.
•	Матрица похожести 10х10: cosine similarity между частотными векторами навыков областей. Используется в формуле domain_sim — чем похожее области, тем выше вклад в score.

` MLOps Engineer  - {Data Science: 0.67, DevOps: 0.15, Backend: 0.08, ...} `
` Flutter Dev     - {Mobile: 0.80, Frontend: 0.15, Backend: 0.05} `

*Почему не вручную? Ручное заполнение онтологии субъективно и не масштабируется. Автоматическое построение из данных адаптируется к реальному употреблению навыков на платформе.*

## 3. Предобработка (тетрадка 2_preprocessing.ipynb)
| Шаг | Что делается | Почему именно так |
| :--- | :---: | ---: |
| Нормализация навыков | Нижний регистр, trim пробелов | 'Python' и 'python' — один навык | 
| Кодирование level | junior=1, middle=2, senior=3, lead=4 | Для сравнения в фильтрации | 
| Кодирование языка/формата | Текст - множества: 'ru+en'→{'ru','en'} | None = совместим с любым — не блокирует | 
| Domain_vector | Профессия - 10-мерный вектор через role→domain mapping | Основа для domain_sim | 
| Experience_norm | log(years+1)/log(21) | Логарифм сглаживает: 1 год vs 5 лет важнее чем 15 vs 20 | 

*Почему логарифм для стажа? Разница между 0 и 3 годами гораздо важнее чем между 15 и 18. Логарифм отражает убывающую отдачу от опыта.*

## 4. Алгоритм скоринга (тетрадка 3_scoring.ipynb)
### 4.1 Жёсткая фильтрация
Выполняется до любых вычислений. Level используется ТОЛЬКО здесь и больше нигде:
•	available = False - исключается
•	level_mentor ≤ level_mentee - исключается
•	Язык несовместим - исключается
•	Формат несовместим - исключается
### 4.2 Четыре фактора
| Фактор | Метод | Формула | При пропуске | 
| :--- | :---: | ---: |
| skill_sim | Jaccard similarity | |A ∩ B| / |A ∪ B| | = 0, штраф w_skill×1.0 |
| domain_sim | Квадратичная форма | v_mentee · M · v_mentor | Всегда есть (fallback вектор) |
| exp_sim | Нормализованный стаж | log(years+1)/log(21) | Всегда есть (0 лет → 0) |
| rating_sim | Нормализованный рейтинг | (rating - 1) / 4 | Всегда есть (default 3.8) | 

### 4.3 Нормализация факторов
Каждый фактор нормализуется в [0,1] через границы p5–p95 из реальных пар, а не подобранные вручную константы:
` norm(x) = max(0, min(1, (x - p5) / (p95 - p5))) `

*Почему p5–p95, а не min-max? Min-max нестабилен к выбросам — один экстремальный ментор сдвинул бы всю шкалу. Перцентили устойчивы и отражают реальный рабочий диапазон значений.*

### 4.4 Наказание за неполный профиль
Если у менти нет навыков — skill_sim = 0 (не исключается и не игнорируется). Веса не перенормируются:
` score = w_skill×0 + w_domain×domain_sim + w_exp×exp_sim + w_rating×rating_sim `

*Почему наказание, а не игнорирование? Если бы мы исключали пустой фактор и перенормировали веса, пользователь без навыков получал бы такие же score как с навыками. Это нечестно — система не может хорошо сматчить человека без данных.*

### 4.5 Персональные веса (4 фактора)
` w_i = 0.10 + 0.60 × (priority_i / sum(priorities)) `

Floor 10% — минимальный вес каждого фактора. Flex 60% распределяется по приоритетам пользователя (1–5 через слайдеры).
*Почему floor 10%? Чтобы пользователь не мог полностью отключить фактор. Если кто-то поставит рейтингу 0 — это не значит что он хочет рекомендации без учёта качества ментора.*

### 4.6 Буст
` final_score = organic_score × (1 + boost_k),  если organic_score ≥ 0.30 `

*Почему порог 0.30? Без порога буст давал бы преимущество нерелевантным менторам которые купили продвижение. Порог гарантирует что ментор хотя бы минимально релевантен прежде чем получить буст.*

## 5. Evaluation (тетрадка 4_evaluation.ipynb)
Ground truth
Для 150 тестовых менти создаётся синтетический идеальный ментор: те же векторы домена и навыков что у менти, уровень на 1 выше, рейтинг 4.9. Алгоритм должен найти его в топ-5 среди 2 000 реальных менторов.
*Почему синтетический ground truth? У нас нет реальной обратной связи от пользователей.*
| Метрика | Что измеряет | Почему важна |
| :--- | :---: | ---: |
| Recall@1 | Идеальный ментор на 1 месте | Насколько хорошо топ-1 рекомендация |
| Recall@3 | Идеальный ментор в топ-3 | Реалистично, так как пользователь смотрит первые 3 сразу | 
| Recall@5 | Идеальный ментор в топ-5 | Максимально мягкая проверка | 
| MRR | Средний обратный ранг | Учитывает позицию, а не только попадание | 
| Coverage | % менторов в выдаче | Нет ли монополии несколькоких менторов | 

## 6. Explainability (тетрадка 5_explainability.ipynb)
Для каждой из 25 000 рекомендаций генерируется три уровня объяснения:

•	Главная причина - одно предложение. Если профиль неполный — явно указываем штраф.
•	Разбивка по факторам — вес каждого в процентах, числовая схожесть, конкретные детали.
•	Детали — совпадающие навыки, что можно изучить у этого ментора, область, стаж, рейтинг.

*Почему важна интерпретируемость? Пользователь которому просто выдали список из 5 имён — не понимает почему именно эти люди. Объяснение повышает доверие и помогает принять решение.*

## 7. Аналитика буста (тетрадка 6_boost_analytics.ipynb)
#### Правильная методология
Буст анализируется на полном пуле кандидатов, а не внутри топ-5. Только так можно увидеть реальный эффект: что было бы без буста и что стало с ним.

*Почему не внутри топ-5? Если бы мы сравнивали позиции внутри уже отобранного топ-5 — все 5 менторов там и так. Нужно смотреть на полный пул из ~800 кандидатов и фиксировать переход через черту.*

#### Интерпретация результатов
•	Если буст помог в <5% случаев — сильные менторы и так в топ-5 органически. Ценность буста для них невысока.
•	Если помог в 10–30% — буст реально двигает позиции, монетизация оправдана.
•	Средний organic_score бустированных в топ-5 должен быть не сильно ниже органических — иначе буст снижает качество выдачи для менти.

